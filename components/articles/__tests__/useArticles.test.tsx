import { renderHook } from "@testing-library/react";
import { useArticles } from "../useArticles";

describe("useArticlesカスタムフックのテスト", () => {
  test("", () => {
    // const { result } = renderHook(() => useArticles(4));

    // console.log({ result });

    expect(true).toBe(true);
  });
});
